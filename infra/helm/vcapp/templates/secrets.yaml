# Placeholder secret. In prod use ExternalSecrets / SealedSecrets / HashiCorp
# Vault Agent Injector. DO NOT deploy this template with real values committed.
{{- if .Values.createDummySecrets }}
apiVersion: v1
kind: Secret
metadata:
  name: vcapp-secrets
type: Opaque
stringData:
  APP_SECRET_KEY: "REPLACE-ME"
  POSTGRES_USER: vcapp
  POSTGRES_PASSWORD: "REPLACE-ME"
  POSTGRES_DB: vcapp
  REDIS_PASSWORD: "REPLACE-ME"
  RABBITMQ_USER: vcapp
  RABBITMQ_PASSWORD: "REPLACE-ME"
  MINIO_ROOT_USER: vcapp
  MINIO_ROOT_PASSWORD: "REPLACE-ME"
  VAULT_TOKEN: "REPLACE-ME"
{{- end }}
