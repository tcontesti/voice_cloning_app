# Private pilot — Windows PC + Spark GPU
# Copy to .env.multihost and adjust. NEVER commit the real file.

# ─── PC-local services ───────────────────────────────────────────────────
POSTGRES_USER=vcapp
POSTGRES_PASSWORD=change-me-pc
POSTGRES_DB=vcapp
REDIS_PASSWORD=change-me-pc
APP_SECRET_KEY=dev-only-change-me-32-bytes-minimum-just-for-pilot
KMS_MOCK_KEY=ZGV2LWtleS0zMi1ieXRlcy1tdXN0LWJlLWxvbmctZW5vdWdoLW9rPT0=

# ─── Spark services (reached via SSH tunnel) ─────────────────────────────
# These MUST match the values in Spark's ~/voice_cloning_app/.env.
RABBITMQ_USER=vcapp
RABBITMQ_PASSWORD=change-me-spark
MINIO_ROOT_USER=vcapp
MINIO_ROOT_PASSWORD=change-me-spark
